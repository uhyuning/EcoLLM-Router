[ app/services/ — 핵심 비즈니스 로직 ]

담당: 하윤

  classifier.py  학습된 ML 모델을 로드해 복잡도 점수를 반환한다
                 (모델 학습 자체는 classifier/ 폴더에서 수행)
  router.py      복잡도 점수를 보고 Flash / Pro 중 어떤 모델을 쓸지 결정한다
  llm_client.py  Gemini API를 비동기로 호출하고 재시도(retry) 로직을 처리한다
